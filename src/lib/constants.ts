export const USER_ROLES = {
  SINGLE: 'single',
  RECOMMENDER: 'recommender',
} as const;

export const RECOMMENDATION_STATUS = {
  PENDING: 'pending',
  ACCEPTED: 'accepted',
  REJECTED: 'rejected',
  CONTACTED: 'contacted',
} as const;
